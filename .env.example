# ──────────────────────────────────────────────────────────────
# GramUrga — Environment Variables Template
# ──────────────────────────────────────────────────────────────
# Copy this file to  .env  (no ".example" suffix) and fill in
# your real values. The .env file is in .gitignore and will
# NEVER be committed to version control.
# ──────────────────────────────────────────────────────────────

# ── Database (MySQL / MariaDB) ──────────────────────────────
DB_HOST=sql312.ezyro.com
DB_NAME=ezyro_41983904_gramurga
DB_USER=ezyro_41983904
DB_PASS=your_actual_password_here

# ── Admin seed password (used by analytics/database_setup.php) ──
# Change this immediately after first run!
ADMIN_DEFAULT_PASS=YourStrongPassword@123
